{
  "scripts": {
    "prettier": "prettier --write '(contracts|interfaces)/**/*.sol'",
    "prettier:list": "prettier --list-different '(contracts|interfaces)/**/*.sol'",
    "prettier:check": "prettier --check '(contracts|interfaces)/**/*.sol'",
    "lint:check": "yarn prettier:check"
  },
  "devDependencies": {
    "hardhat": "^2.8.2",
    "prettier": "^2.5.1",
    "prettier-plugin-solidity": "^1.0.0-beta.19"
  }
}
